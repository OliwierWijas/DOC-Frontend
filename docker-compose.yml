version: "3.8"
services:
  frontend-doc:
    build:
      context: ./
    container_name: 'assignment-frontend-container'
    image: 'frontend:v2'
    ports:
      - 3000:3000

volumes:
  db:
    driver: local